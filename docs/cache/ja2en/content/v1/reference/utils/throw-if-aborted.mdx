---
title: throwIfAborted
slug: en/v1/reference/utils/throw-if-aborted
---

`throwIfAborted` is a function that throws an error if the provided abort signal has been aborted. It is provided for environments where `new AbortSignal().throwIfAborted()` is not implemented.

## Import

```ts
import { throwIfAborted } from "@tai-kun/surrealdb/utils";
```

## Usage

```ts
function throwIfAborted(signal: AbortSignal | null | undefined): void;
```

#### Arguments

`signal`
:   The abort signal.

#### Return Value

None.
