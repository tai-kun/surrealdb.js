---
title: throwIfAborted
slug: ko/v1/reference/utils/throw-if-aborted
---

`throwIfAborted`는 전달된 취소 신호가 취소된 경우 그 이유를 throw하는 함수입니다. `new AbortSignal().throwIfAborted()`가 구현되지 않은 환경을 위해 제공됩니다.

## 가져오기

```ts
import { throwIfAborted } from "@tai-kun/surrealdb/utils";
```

## 사용 방법

```ts
function throwIfAborted(signal: AbortSignal | null | undefined): void;
```

#### 인수

`signal`
:   취소 신호입니다.

#### 반환값

없음.
