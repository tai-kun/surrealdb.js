---
title: GeometryCollection
slug: ko/v2/api/data/geometry-collection
---

import Badge from "~/components/ko/Badge.astro";

편집 중입니다.

### 가져오기

```ts
import { GeometryCollection } from "@tai-kun/surrealdb/data-types/decode-only";
import { GeometryCollection } from "@tai-kun/surrealdb/data-types/encodable";
import { GeometryCollection } from "@tai-kun/surrealdb/data-types/standard";
```

### `.constructor()`

새로운 `GeometryCollection` 객체를 생성합니다.

```ts
new GeometryCollection(source);
```

#### 인수

##### `source`

편집 중입니다.

#### 반환 값

`new`를 통해 호출될 때 `GeometryCollection`은 해당 인스턴스를 반환합니다.
