---
title: 预先准备的查询
slug: zh-CN/v1/guides/prepared-query
---

## 基本用法

以下是使用预先准备的查询的基本示例：

```ts
import { surql, Surreal, Thing } from "@tai-kun/surrealdb";

const CreateUserQuery = surql`CREATE ONLY user:foo CONTENT { age: 42 };`
  .returns<[{ id: Thing<"user">, age: number }]>();

const db = new Surreal();
await db.connect(`ws://localhost:8000`);
await db.signin({ user: "root", pass: "root" });
await db.use("example", "example");

const results = await db.query(CreateUserQuery);
//    ^? const results: [{ id: Thing<"user">, age: number }]

await db.disconnect();
```

预先准备查询可以有效利用类型推断，但需要手动设置推断的类型。

## 验证查询结果

通过将查询结果验证器传递给 `.returns` 的参数，可以使查询更安全。以下示例使用 zod 来验证查询结果是否符合预期：

```ts
import { surql, Surreal, Thing } from "@tai-kun/surrealdb";
import { z } from "zod";

const isUserTable = (id: Thing): id is Thing<"user"> => id.tb === "user";

const CreatedUserSchema = z.tuple([
  z.object({
    id: z.instanceof(Thing).refine(isUserTable),
    age: z.number(),
  }),
]);

const CreateUserQuery = surql`CREATE ONLY user:bar CONTENT { age: 42 };`
  .returns(CreatedUserSchema.parse.bind(CreatedUserSchema));

const db = new Surreal();
await db.connect(`ws://localhost:8000`);
await db.signin({ user: "root", pass: "root" });
await db.use("example", "example");

const results = await db.query(CreateUserQuery);
//    ^? const results: [{ id: Thing<"user">, age: number }]

await db.disconnect();
```

并非必须依赖 zod，可以使用 valibot 或通用函数来验证响应。

## 在查询中嵌入变量

`surql` 可以使用模板字符串来编写查询，因此可以直观地嵌入值：

```ts
import { surql, Surreal, Thing } from "@tai-kun/surrealdb";

const USERNAME = "baz";
const USER_AGE = 42;

const CreateUserQuery = surql`
  CREATE ONLY type::thing('user', ${USERNAME}) CONTENT { age: ${USER_AGE} };`
  .returns<[{ id: Thing<"user">; age: number }]>();

const db = new Surreal();
await db.connect(`ws://localhost:8000`);
await db.signin({ user: "root", pass: "root" });
await db.use("example", "example");

const results = await db.query(CreateUserQuery);
//    ^? const results: [{ id: Thing<"user">, age: number }]

await db.disconnect();
```

以上示例发送的 RPC 请求参数如下：

```text
CREATE ONLY type::thing('user', $_jst_0) CONTENT { age: $_jst_1 };
```

```ts
{
  _jst_0: "baz",
  _jst_1: 42
}
```

## 定义查询参数

可以使用 `surql.slot` 为预先定义的查询定义参数：

```ts
import { surql, Surreal, Thing } from "@tai-kun/surrealdb";
import { z } from "zod";

const isUserTable = (id: Thing): id is Thing<"user"> => id.tb === "user";

const UserIdSchema = z.instanceof(Thing).refine(isUserTable);

const UserIdSlot = surql.slot("id")
  .type(UserIdSchema.parse.bind(UserIdSchema));

const UserAgeSlot = surql.slot("age", 42);

const CreateUserQuery = surql`
  CREATE ONLY ${UserIdSlot} CONTENT { age: ${UserAgeSlot} };`
  .returns<[{ id: Thing<"user">; age: number }]>();

const db = new Surreal();
await db.connect(`ws://localhost:8000`);
await db.signin({ user: "root", pass: "root" });
await db.use("example", "example");

const results = await db.query(CreateUserQuery, {
  id: new Thing("user", "tai-kun"),
});

await db.disconnect();
```

每个 slot 都需要一个变量名，使用 `.type()` 方法可以对变量进行类型级别约束。与 `.returns()` 类似，可以将验证值的函数传递给 `.type()` 的参数。以上示例中，变量名为 `id` 的 slot 必须是表名为 `"user"` 的记录 ID。

slot 的参数可以设置为变量名后跟默认值。如果在执行时省略 slot 的变量名，将使用此默认值。以上示例中，变量名为 `age` 的 slot 设置了默认值 `42`。

除了 `.type()` 之外，slot 还可以使用 `.rename()`、`.default()`、`.optional()`、`.required()` 等方法。

如果预先定义的查询包含在执行时必须指定的变量的 slot，并且未指定该变量，则查询将在执行时被 `SurrealTypeError` 拒绝。如果 TypeScript 中的类型推断正确，即使不执行，也可以在类型级别上显示错误。例如，以下代码省略了必须的变量，并在使用 `tsc` 进行类型检查时会导致错误：

```ts
import { surql, Surreal, Thing } from "@tai-kun/surrealdb";

const UserIdSlot = surql.slot("id")
  .type<Thing<"user">>();

const UserAgeSlot = surql.slot("age", 42);

const CreateUserQuery = surql`
  CREATE ONLY ${UserIdSlot} CONTENT { age: ${UserAgeSlot} };`
  .returns<[{ id: Thing<"user">, age: number }]>()

const db = new Surreal();
await db.connect("ws://localhost:8000");

const results = await db.query(CreateUserQuery, {
  // id: new Thing("user", "tai-kun"),
});

await db.disconnect();
```

`npx tsc --noEmit`:

![tsc](./tsc.png)
