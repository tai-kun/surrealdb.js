---
title: makeAbortApi
slug: zh-CN/v1/reference/utils/make-abort-api
---

`makeAbortApi` 是一个用于创建中止信号及其关联中止函数的函数。

### 导入

```ts
import { makeAbortApi } from "@tai-kun/surrealdb/utils";
```

### 用法

```ts
function makeAbortApi(signal?: AbortSignal): [
  signal: AbortSignal,
  abort: (reason?: unknown) => void,
];
```

#### 参数

##### `signal`

要关联的中止信号。

#### 返回值

返回一个按以下顺序排列的数组：

##### `signal`

中止信号。初始状态下不会被中断。

##### `abort`

在中止信号上发出 `"abort"` 事件。可以可选地接收一个 `reason` 参数，用于通过 `"abort"` 事件传递中止原因。

#### 示例

以下示例创建了一个不带参数的 Abort API：

```ts
import { makeAbortApi } from "@tai-kun/surrealdb/utils";

const [signal, abort] = makeAbortApi();
abort();
```

上面的示例与 `new AbortController()` 几乎相同。与 `AbortController` 的不同之处在于 `makeAbortApi` 返回的是一个数组而不是一个对象。

以下示例指定要关联的中止信号来创建 Abort API：

```ts
import { makeAbortApi } from "@tai-kun/surrealdb/utils";

const controller = new AbortController();
controller.signal.addEventListener("abort", function() {
  console.log("controller.signal", this.reason);
});

const [signal] = makeAbortApi(controller.signal);
signal.addEventListener("abort", function() {
  console.log("signal", this.reason);
});

controller.abort("test");
```

输出：

```js
controller.signal test
signal test
```

中止事件会从 Abort API 的中止传播到关联的中止信号：

```ts
import { makeAbortApi } from "@tai-kun/surrealdb/utils";

const controller = new AbortController();
controller.signal.addEventListener("abort", function() {
  console.log("controller.signal", this.reason);
});

const [signal, abort] = makeAbortApi(controller.signal);
signal.addEventListener("abort", function() {
  console.log("signal", this.reason);
});

abort("test");
```

输出：

```js
controller.signal test
signal test
```
