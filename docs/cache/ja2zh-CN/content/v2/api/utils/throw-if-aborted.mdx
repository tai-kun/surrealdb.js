---
title: throwIfAborted
slug: zh-CN/v2/api/utils/throw-if-aborted
---

`throwIfAborted` 是一个函数，当传递给它的中止信号被中止时，它会抛出中止的原因。它用于在没有实现 `new AbortSignal().throwIfAborted()` 的环境中。

## 导入

```ts
import { throwIfAborted } from "@tai-kun/surrealdb/utils";
```

## 用法

```ts
function throwIfAborted(signal: AbortSignal | null | undefined): void;
```

#### 参数

`signal`
:   中止信号。

#### 返回值

无。
