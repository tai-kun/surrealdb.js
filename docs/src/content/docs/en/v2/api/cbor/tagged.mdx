---
title: Tagged
slug: en/v2/api/cbor/tagged
---

import Badge from "~/components/en/Badge.astro";

`Tagged` represents a custom value identified by an integer tag ranging from 0 to 2^64-1.

### Import

```ts
import { Tagged } from "@tai-kun/surrealdb/cbor";
```

### `.constructor()`

```ts
new Tagged<T>(tag: number | BigInt, value: T);
```

#### Arguments

##### `tag`

An integer value from 0 to 2^64-1 used to identify the tagged data item. Note that in JavaScript, use `BigInt` for numbers exceeding 2^53-1.

##### `value`

The value of the tagged data item.

#### Return Value

When called through `new`, `Tagged` returns an instance of itself.

### `.tag` <Badge variant="instance" /><Badge variant="property" />

`.tag` is a property that holds the integer value from 0 to 2^64-1 used to identify the tagged data item.

### `.value` <Badge variant="instance" /><Badge variant="property" />

`.value` is a property that stores the value of the tagged data item.

### `.toCBOR()` <Badge variant="instance" /><Badge variant="method" />

`.toCBOR` is a function that returns information for encoding the data into CBOR.

```ts
toCBOR(): [tag: number | bigint, value: unknown];
```

#### Arguments

None.

#### Return Value

Returns information for encoding the data into CBOR.

### Remarks

Refer to the following documentation for additional information:

- [https://www.iana.org/assignments/cbor-tags/cbor-tags.xhtml](https://www.iana.org/assignments/cbor-tags/cbor-tags.xhtml)
