---
title: isSafeNumber
slug: en/v2/api/utils/is-safe-number
---

`isSafeNumber` verifies whether a given number is safe for mathematical operations.

### Import

```ts
import { isSafeNumber } from "@tai-kun/surrealdb/utils";
```

### Usage

```ts
function isSafeNumber(value: number): boolean;
```

#### Arguments

##### `value`

The number to be validated.

#### Return Value

Returns `false` if `value` is:

- `NaN`
- Infinity
- Outside the range of ±2^53-1
