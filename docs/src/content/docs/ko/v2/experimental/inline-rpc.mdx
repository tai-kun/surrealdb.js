---
title: 인라인 RPC
slug: ko/v2/experimental/inline-rpc
banner:
  content: 이것은 실험적인 기능이며 API는 자주 변경될 수 있습니다.
---

인라인 RPC는 `Surreal` 클래스의 인스턴스를 생성하지 않고도 즉석에서 일회성 RPC를 실행하는 함수입니다. `.rpc()` 메서드와 사용법이 거의 동일합니다. 단, 기본적으로 `JsonFormatter`가 사용된다는 점에 유의하십시오.

### 가져오기

```ts
import { rpc } from "@tai-kun/surrealdb";
```

### 예

```ts
import { rpc } from "@tai-kun/surrealdb";

const token = await rpc("http://127.0.0.1:8000", "signin", {
  params: [{
    user: "root",
    pass: "root",
  }],
});
```
