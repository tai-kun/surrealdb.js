---
title: 内联查询
slug: zh-CN/v1/experimental/inline-query
banner:
  content: 这是一个实验性功能，API 可能会频繁更改。
---

内联查询无需创建 `Surreal` 类实例即可执行一次性的自定义查询。使用方法与 [`.query()`](/zh-CN/guides/querying#query) 方法几乎相同。但请注意，默认情况下使用的是 `JsonFormatter`。

### 导入

```ts
import { query } from "@tai-kun/surrealdb";
```

### 示例

```ts
import { query, rpc } from "@tai-kun/surrealdb";

const token = await rpc("http://127.0.0.1:8000", "signin", {
  params: [{
    user: "root",
    pass: "root",
  }],
});

const results = await query("http://127.0.0.1:8000", "RETURN 42", {}, {
  token,
  namespace: "test",
  database: "test",
});
```
