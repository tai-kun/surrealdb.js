---
title: throwIfAborted
slug: zh-CN/v1/reference/utils/throw-if-aborted
---

`throwIfAborted` 函数用于检查传入的中止信号是否处于中止状态，如果是则抛出错误。该函数是为了兼容不支持 `new AbortSignal().throwIfAborted()` 的环境而提供的。

## 导入

```ts
import { throwIfAborted } from "@tai-kun/surrealdb/utils";
```

## 使用方法

```ts
function throwIfAborted(signal: AbortSignal | null | undefined): void;
```

#### 参数

`signal`
:   中止信号。

#### 返回值

无。
