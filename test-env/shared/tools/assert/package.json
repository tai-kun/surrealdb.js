{
  "name": "@tools/assert",
  "type": "module",
  "main": "index.js",
  "dependencies": {
    "assert": "^2.1.0",
    "esbuild": "^0.23.0",
    "esbuild-plugin-polyfill-node": "^0.3.0"
  },
  "scripts": {
    "//": "postinstall 実行時には tools/assert にいるので、まず 2 階層上がる必要がある。",
    "postinstall": "cd ../../node_modules/@tools/assert && node browserify.js"
  }
}
